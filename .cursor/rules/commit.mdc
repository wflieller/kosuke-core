---
description: Commit Changes
globs: **
---

Run the following commands when asked to commit the changes you have made:

```bash
npm run test
git add --all
git status
git commit -m "{commit_message}" # based on the diffs and changes you have made
git push
```

and fix all the errors or warnings generated by the commands above.
